import React from "react";

const JSXRules = () => {
  return (
    <div>
      <h1>JSX Rules</h1>
      <p>Here are some important JSX rules:</p>
      <ul>
        <li>JSX must return a single parent element.</li>
        <li>JSX elements must be properly closed.</li>
        <li>JSX attributes use camelCase (e.g., className instead of class).</li>
      </ul>
    </div>
  );
};

export default JSXRules;
